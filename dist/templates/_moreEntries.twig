{# Set parameters for prev/next elements list #} {% set params = craft.entries.relatedTo(category).order('postDate asc') %} {# Get the prev/next elements #} {% set prevEntry = entry.getPrev(params) %} {% set nextEntry = entry.getNext(params) %} {% if prevEntry or nextEntry %} <nav class="moreEntries">
    <ul class="moreEntries_list unstyledList"> {# And make sure to only output the links if the element exists #} {% if nextEntry %} <li class="moreEntries_next">
            <span class="moreEntries_subhead">Next {{ categorySlug|trim('s', 'right') }}: </span>
            <a href="{{ nextEntry.url }}" title="Next entry: {{ nextEntry.title }}" class="moreEntries_link">
            {{ nextEntry.title }}
            </a>
        </li> {% endif %} {% if prevEntry %} <li class="moreEntries_previous">
            <span class="moreEntries_subhead">Previous {{ categorySlug|trim('s', 'right') }}: </span>
            <a href="{{ prevEntry.url }}" title="Previous entry: {{ prevEntry.title }}" class="moreEntries_link">
            {{ prevEntry.title }}
            </a>
        </li> {% endif %} </ul>
</nav> {% endif %}