{#
 # Category display
 # ---------------
 # Template for showing a groups of categories entries
 #} {% extends "_layout" %} {% set pageTitle = 'assorted writing by bps' %} {% set description = '' %} {% set categorySlug = craft.app.request.getSegment(1) %} {% set category = craft.categories.slug(categorySlug) %} {% set pageID = categorySlug %} {% block content %} <header>
    <h2 class="subheading">Work</h2>
    <h1 class="heading">{{ categorySlug }}</h1>
</header> {% set entries = craft.entries.section('words').relatedTo(category).find() %} {% if entries|length %} <ul id="entryDirectory"> {% for entry in entries %} {% include '_entrySyndicated' with { 'categorySlug' : categorySlug } %} {% endfor %} </ul> {% endif %} </ul> {% endblock %}